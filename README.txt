1. mainx.py and remiguix.py should be used if classes are created outside the BankStatementAnalyzer(App) class.

2. main.py should be used if all the GUI is inside the BankStatementAnalyzer class.

Both return the same results. #1 is more structured into different py files. #2 is very long.